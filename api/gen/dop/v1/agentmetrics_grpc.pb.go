// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: dop/v1/agentmetrics.proto

package dopv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	AgentMetricsService_RecordTurns_FullMethodName          = "/dop.v1.AgentMetricsService/RecordTurns"
	AgentMetricsService_GetDemandConsumption_FullMethodName = "/dop.v1.AgentMetricsService/GetDemandConsumption"
)

// AgentMetricsServiceClient is the client API for AgentMetricsService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// The agent's consumption metrics (P-23 phase 1).
//
// Who calls this is NOT the edge: it is the collector, a container beside the
// agent in the sandbox's pod, pushing what Claude Code writes to disk. It signs
// its calls as the caller `collector` (ADR-0022), and the core lets that caller
// reach THIS service and nothing else — a stolen collector key is worth
// polluting telemetry, never reading an account.
//
// The turns are pushed in batches as they appear, which is what makes the
// cockpit's consumption live instead of a number that shows up when the demand
// ends.
type AgentMetricsServiceClient interface {
	// RecordTurns ingests a batch. Idempotent by (session, turn uuid): the
	// collector re-reads the tail of a file that is still being written, so the
	// same turn arriving twice is the normal case, not an error.
	RecordTurns(ctx context.Context, in *RecordTurnsRequest, opts ...grpc.CallOption) (*RecordTurnsResponse, error)
	// Consumption of one demand, aggregated. It is a QUERY and not a projection
	// on purpose: the aggregates that matter change as the questions change, and
	// a stored aggregate ages and lies.
	GetDemandConsumption(ctx context.Context, in *GetDemandConsumptionRequest, opts ...grpc.CallOption) (*DemandConsumption, error)
}

type agentMetricsServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewAgentMetricsServiceClient(cc grpc.ClientConnInterface) AgentMetricsServiceClient {
	return &agentMetricsServiceClient{cc}
}

func (c *agentMetricsServiceClient) RecordTurns(ctx context.Context, in *RecordTurnsRequest, opts ...grpc.CallOption) (*RecordTurnsResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RecordTurnsResponse)
	err := c.cc.Invoke(ctx, AgentMetricsService_RecordTurns_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *agentMetricsServiceClient) GetDemandConsumption(ctx context.Context, in *GetDemandConsumptionRequest, opts ...grpc.CallOption) (*DemandConsumption, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(DemandConsumption)
	err := c.cc.Invoke(ctx, AgentMetricsService_GetDemandConsumption_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// AgentMetricsServiceServer is the server API for AgentMetricsService service.
// All implementations must embed UnimplementedAgentMetricsServiceServer
// for forward compatibility.
//
// The agent's consumption metrics (P-23 phase 1).
//
// Who calls this is NOT the edge: it is the collector, a container beside the
// agent in the sandbox's pod, pushing what Claude Code writes to disk. It signs
// its calls as the caller `collector` (ADR-0022), and the core lets that caller
// reach THIS service and nothing else — a stolen collector key is worth
// polluting telemetry, never reading an account.
//
// The turns are pushed in batches as they appear, which is what makes the
// cockpit's consumption live instead of a number that shows up when the demand
// ends.
type AgentMetricsServiceServer interface {
	// RecordTurns ingests a batch. Idempotent by (session, turn uuid): the
	// collector re-reads the tail of a file that is still being written, so the
	// same turn arriving twice is the normal case, not an error.
	RecordTurns(context.Context, *RecordTurnsRequest) (*RecordTurnsResponse, error)
	// Consumption of one demand, aggregated. It is a QUERY and not a projection
	// on purpose: the aggregates that matter change as the questions change, and
	// a stored aggregate ages and lies.
	GetDemandConsumption(context.Context, *GetDemandConsumptionRequest) (*DemandConsumption, error)
	mustEmbedUnimplementedAgentMetricsServiceServer()
}

// UnimplementedAgentMetricsServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedAgentMetricsServiceServer struct{}

func (UnimplementedAgentMetricsServiceServer) RecordTurns(context.Context, *RecordTurnsRequest) (*RecordTurnsResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method RecordTurns not implemented")
}
func (UnimplementedAgentMetricsServiceServer) GetDemandConsumption(context.Context, *GetDemandConsumptionRequest) (*DemandConsumption, error) {
	return nil, status.Error(codes.Unimplemented, "method GetDemandConsumption not implemented")
}
func (UnimplementedAgentMetricsServiceServer) mustEmbedUnimplementedAgentMetricsServiceServer() {}
func (UnimplementedAgentMetricsServiceServer) testEmbeddedByValue()                             {}

// UnsafeAgentMetricsServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to AgentMetricsServiceServer will
// result in compilation errors.
type UnsafeAgentMetricsServiceServer interface {
	mustEmbedUnimplementedAgentMetricsServiceServer()
}

func RegisterAgentMetricsServiceServer(s grpc.ServiceRegistrar, srv AgentMetricsServiceServer) {
	// If the following call panics, it indicates UnimplementedAgentMetricsServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&AgentMetricsService_ServiceDesc, srv)
}

func _AgentMetricsService_RecordTurns_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RecordTurnsRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AgentMetricsServiceServer).RecordTurns(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AgentMetricsService_RecordTurns_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AgentMetricsServiceServer).RecordTurns(ctx, req.(*RecordTurnsRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AgentMetricsService_GetDemandConsumption_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetDemandConsumptionRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AgentMetricsServiceServer).GetDemandConsumption(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AgentMetricsService_GetDemandConsumption_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AgentMetricsServiceServer).GetDemandConsumption(ctx, req.(*GetDemandConsumptionRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// AgentMetricsService_ServiceDesc is the grpc.ServiceDesc for AgentMetricsService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var AgentMetricsService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "dop.v1.AgentMetricsService",
	HandlerType: (*AgentMetricsServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "RecordTurns",
			Handler:    _AgentMetricsService_RecordTurns_Handler,
		},
		{
			MethodName: "GetDemandConsumption",
			Handler:    _AgentMetricsService_GetDemandConsumption_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "dop/v1/agentmetrics.proto",
}
