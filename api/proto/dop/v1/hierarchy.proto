syntax = "proto3";
package dop.v1;

import "dop/v1/common.proto";

message Workspace {
  string     id          = 1;
  AccountRef account     = 2;
  string     name        = 3;
  string     key         = 4;
  string     description = 5;
  repeated string tags   = 6;
  AuditStamp audit       = 7;
}

message Project {
  string       id        = 1;
  WorkspaceRef workspace = 2;
  string       name      = 3;
  string       description = 4;
  // Attached resources: repos, task manager, git flow, workflow, skills.
  repeated ProjectRepo  repos        = 5;
  ProjectTaskManager    task_manager = 6;
  repeated ResourceRef  resources    = 7;
  repeated string       rules        = 8;
  AuditStamp   audit     = 9;
}

// The provider belongs to the REPOSITORY, not to the project (ADR-0009).
message ProjectRepo {
  string      id           = 1;
  ResourceRef integration  = 2;
  string      external_id  = 3;
  string      name         = 4;
  string      default_branch = 5;
  repeated string pr_targets = 6;
}

message ProjectTaskManager {
  ResourceRef integration       = 1;
  string      external_space_id = 2;   // the provider's space (never a bare "workspace")
  string      external_project_id = 3;
  repeated string card_types    = 4;   // dynamic, coming from the provider
}

service HierarchyService {
  rpc ListWorkspaces (ListWorkspacesRequest) returns (ListWorkspacesResponse);
  rpc GetWorkspace   (GetWorkspaceRequest)   returns (Workspace);
  rpc CreateWorkspace(CreateWorkspaceRequest)returns (Workspace);
  rpc UpdateWorkspace(UpdateWorkspaceRequest)returns (Workspace);
  rpc ListProjects   (ListProjectsRequest)   returns (ListProjectsResponse);
  rpc GetProject     (GetProjectRequest)     returns (Project);
  rpc CreateProject  (CreateProjectRequest)  returns (Project);
  rpc UpdateProject  (UpdateProjectRequest)  returns (Project);
  // The cockpit's tree in one call.
  rpc GetTree        (GetTreeRequest)        returns (GetTreeResponse);
}

message ListWorkspacesRequest  { reserved 1; PageRequest page = 2; }
message ListWorkspacesResponse { repeated Workspace workspaces = 1; PageResponse page = 2; }
message GetWorkspaceRequest    { reserved 1; string id = 2; }
message CreateWorkspaceRequest { reserved 1; string name = 2; string key = 3; string description = 4; repeated string tags = 5; string idempotency_key = 6; }
message UpdateWorkspaceRequest { reserved 1; Workspace workspace = 2; }
message ListProjectsRequest    { reserved 1; WorkspaceRef workspace = 2; }
message ListProjectsResponse   { repeated Project projects = 1; }
message GetProjectRequest      { reserved 1; string id = 2; }
message CreateProjectRequest   { reserved 1; WorkspaceRef workspace = 2; string name = 3; string description = 4; string idempotency_key = 5; }
message UpdateProjectRequest   { reserved 1; Project project = 2; }
message GetTreeRequest         { reserved 1; }
message GetTreeResponse {
  message Node {
    Workspace workspace = 1;
    repeated Project projects = 2;
  }
  repeated Node nodes = 1;
}
